---
id: T2.3
title: Structured logging with request IDs
plan: plan-2026-03-plan-saas-readiness
type: feature
priority: P1
complexity: 30
status: done
sprint: 2
tags:
- observability
- logging
depends_on: []
completed_at: '2026-03-03T20:45:55.278505'
---

# Objective

Replace ad-hoc logging with structured JSON logs and per-request correlation IDs for production traceability.

# Files to Create/Update

| File | Change |
|------|--------|
| `src/modules/credit/logging_config.py` | New: configure structlog with JSON/console renderers |
| `src/modules/credit/middleware.py` | New: request ID middleware (X-Request-ID header) |
| `src/modules/credit/router.py` | Add middleware, use structured logger |
| `src/modules/credit/assessment.py` | Use structured logger |
| `src/modules/credit/dispute_pathway.py` | Use structured logger |
| `pyproject.toml` | Add structlog dependency |
| `src/modules/credit/tests/test_logging.py` | New: test JSON output and request ID propagation |

# Implementation Plan

1. Add `structlog>=24.0` to pyproject.toml
2. Create `logging_config.py`: structlog with JSON renderer (prod) / console renderer (dev)
3. Create `middleware.py`: FastAPI middleware generating/propagating X-Request-ID, binding to structlog context
4. Update `router.py`: add middleware, log request/response with timing
5. Update `assessment.py` and `dispute_pathway.py`: use structured logger
6. Write tests verifying JSON format and request ID propagation

# Acceptance Criteria

- [x] All log output is JSON in production mode
- [x] Every request gets a unique `request_id` in logs
- [x] X-Request-ID header returned in responses
- [x] Log entries include: timestamp, level, request_id, message
- [x] Console-friendly output in development mode
- [x] Tests pass locally (`pytest`)

# Verification

```bash
ENVIRONMENT=production python -c "from modules.credit.logging_config import get_logger; log=get_logger(); log.info('test')"
```