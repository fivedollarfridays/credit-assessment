---
id: T17.1
title: 'Expand ORM models with missing tables and columns'
plan: plan-2026-03-sprint-17
type: refactor
priority: P0
complexity: 55
status: pending
sprint: '17'
tags: [database, schema]
depends_on: []
---

# Objective

Add all missing ORM models and expand existing ones to support full DB persistence.

# Implementation Plan

## Expand existing models in models_db.py

1. **User model** — add `role` (String), `org_id` (String) columns
2. **AssessmentRecord** — add `user_id` (String), `org_id` (String) columns

## Create new ORM models

3. **Subscription** — email, subscription_id, status, plan, created_at, updated_at
4. **ConsentRecord** — user_id, consent_version, consented_at (unique on user_id+version)
5. **UserAssessment** — user_id, assessment_data (JSON), recorded_at
6. **WebhookRegistration** — id, url, events (JSON), secret, owner_id, is_active, created_at
7. **WebhookDelivery** — webhook_id (FK), event_type, status, status_code, timestamp
8. **ApiKey** — key (PK), org_id, role, expires_at, created_at, revoked_at
9. **FeatureFlag** — key (PK), description, enabled, targeting (JSON), created_at, updated_at

# Acceptance Criteria

- [ ] User model has role and org_id columns
- [ ] AssessmentRecord has user_id and org_id columns
- [ ] 7 new ORM models created (Subscription, ConsentRecord, UserAssessment, WebhookRegistration, WebhookDelivery, ApiKey, FeatureFlag)
- [ ] All models use proper types, constraints, and indexes
- [ ] models_db.py stays under 400 lines (split if needed)
- [ ] Tests pass
- [ ] 0 ruff issues

# Verification

- `python -m pytest src/ -q`
- `ruff check src/ && ruff format --check src/`
- `bpsai-pair arch check src/modules/credit/models_db.py`
