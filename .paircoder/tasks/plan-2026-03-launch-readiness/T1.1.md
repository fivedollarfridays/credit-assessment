---
id: T1.1
title: "Add missing symbols to types.py (ConfidenceLevel, EligibilityStatus, thresholds)"
plan: plan-2026-03-launch-readiness
status: pending
priority: P0
complexity: 20
---

## Objective

Add the 4 missing symbols to `src/modules/credit/types.py` that `assessment.py` and `dispute_pathway.py` import but don't exist yet. This single task unblocks ~103 test failures/errors.

## What to Add

1. **`ConfidenceLevel`** — `str, Enum` with values `HIGH = "high"`, `MEDIUM = "medium"`, `LOW = "low"`
   - Used in `assessment.py:249-259` for `ThresholdEstimate.confidence`
2. **`EligibilityStatus`** — `str, Enum` with values `ELIGIBLE = "eligible"`, `BLOCKED = "blocked"`
   - Used in `assessment.py:280,290` for `EligibilityItem.status`
3. **`HIGH_UTILIZATION_THRESHOLD = 75`** — numeric constant
   - Used in `assessment.py:117` and `dispute_pathway.py:194`
4. **`MODERATE_UTILIZATION_THRESHOLD = 50`** — numeric constant
   - Used in `assessment.py:153,305`

## Where to Add

- Enums: after existing enums (after `ActionPriority`, before `# --- Pydantic Models ---`)
- Constants: in the `# --- Constants ---` section alongside `SCORE_WEIGHTS`, `SCORE_BANDS`, etc.

## Also Update

- `__init__.py`: Add new symbols to imports and `__all__`

## Acceptance Criteria

- [ ] `from modules.credit.types import ConfidenceLevel` works
- [ ] `from modules.credit.types import EligibilityStatus` works
- [ ] `from modules.credit.types import HIGH_UTILIZATION_THRESHOLD` returns 75
- [ ] `from modules.credit.types import MODERATE_UTILIZATION_THRESHOLD` returns 50
- [ ] `assessment.py` imports without error
- [ ] `dispute_pathway.py` imports without error
- [ ] All existing 152 passing tests still pass
- [ ] Tests in `test_assessment.py`, `test_dispute_pathway.py`, `test_integration.py` now import successfully

## Dependencies

- None (first task)
