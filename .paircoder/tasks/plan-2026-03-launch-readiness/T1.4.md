---
id: T1.4
title: "Get all 255 tests green and coverage to 100%"
plan: plan-2026-03-launch-readiness
status: pending
priority: P0
complexity: 30
---

## Objective

After T1.1, T1.2, and T1.3, run the full test suite and fix any remaining failures. Ensure `pytest --cov` reports 100% coverage (as required by `pyproject.toml`'s `fail_under = 100`).

## Expected State After T1.1 + T1.2

- All ImportError cascades resolved (T1.1)
- All validation tests pass (T1.2)
- `slowapi` declared (T1.3)

## Likely Remaining Work

1. **Coverage gaps in `types.py`** — The new `ConfidenceLevel` and `EligibilityStatus` enums need to be exercised by tests. The existing test files (`test_types.py`, `test_enums.py`) may already cover enum values, but verify.
2. **`ThresholdEstimate.confidence` field type** — Currently `str = "medium"`. May need to change to `ConfidenceLevel = ConfidenceLevel.MEDIUM` for type consistency with how `assessment.py` uses it. Verify tests pass.
3. **`EligibilityItem.status` field type** — Currently `str`. May need to change to `EligibilityStatus` for consistency with how `assessment.py` uses it. Verify tests still compare to `"eligible"` / `"blocked"` strings.
4. **`CreditReadiness.score_band` field type** — Currently `str`. Tests compare to `"good"` etc. `get_score_band()` returns `ScoreBand` enum. Since `ScoreBand` is `str, Enum`, equality with string values works. Verify.

## Process

1. Run `pytest --cov --cov-report=term-missing -q`
2. Identify any remaining failures or coverage misses
3. Fix each issue
4. Re-run until all green + 100% coverage

## Acceptance Criteria

- [ ] `pytest` — all 255+ tests pass (0 failures, 0 errors)
- [ ] `pytest --cov` — 100% coverage (meets `fail_under = 100`)
- [ ] No test file changes that weaken assertions
- [ ] `bpsai-pair arch check` passes on all modified files

## Dependencies

- Requires T1.1, T1.2, T1.3
