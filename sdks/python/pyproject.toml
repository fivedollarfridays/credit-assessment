[project]
name = "credit-assessment-client"
version = "1.0.0"
description = "Python SDK for the Credit Assessment API"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27"]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
